use crate::smart_house::Error;

/// Термометр
#[allow(unused)]
pub struct Thermometer {}

impl Thermometer {
    /// текущая температура в градусах Цельсия
    pub fn _celsius_temperature(&self) -> Result<f32, Error> {
        todo!();
    }
}
